name: Deploy to GitHub Pages

on:
  push:
    branches: ["main"] # Or "master" depending on your branch name

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build_site:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: gift_magui/package-lock.json

      - name: Install dependencies
        run: npm install
        working-directory: gift_magui

      - name: Build
        run: npm run build
        working-directory: gift_magui

      - name: Upload Artifacts
        uses: actions/upload-pages-artifact@v3
        with:
          # this should point to your dist folder
          path: gift_magui/dist/

  deploy:
    needs: build_site
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
